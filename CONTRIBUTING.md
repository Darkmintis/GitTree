# Contributing

1. Fork the repo
2. Run `npm install && cd webview && npm install`
3. Run `npm run dev` for extension, `cd webview && npm run dev` for UI
4. Submit a PR with a clear description

Keep changes small and focused. Match existing code style. No emoji in code.
